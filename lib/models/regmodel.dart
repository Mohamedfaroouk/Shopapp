import 'package:quiver/core.dart';
/* 
class RegData {
  final String name;
  final String password;
  final String myname;
  final String myphone;

  RegData({
    required this.name,
    required this.password,
    required this.myname,
    required this.myphone,
  });

  @override
  String toString() {
    return '$runtimeType($name, $password,$myname,$myphone)';
  }

  @override
  bool operator ==(Object other) {
    if (other is RegData) {
      return name == other.name &&
          password == other.password &&
          myphone == other.myphone &&
          myname == other.myname;
    }
    return false;
  }

  @override
  int get hashCode => hash4(name, password, myname, myphone);
}
 */